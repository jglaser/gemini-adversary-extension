# Adversarial Hardener Extension
This extension adds the `/harden` command.
It runs a Python script that pits a "Saboteur" agent against an "Engineer" agent.
When the user runs `/harden <file>`, analyze the output log to explain what bugs were found and how they were fixed.
